'use client';import { useState, useEffect, useRef } from 'react';import { motion, AnimatePresence } from 'framer-motion';import {  TbMail,  TbBrandLinkedin,  TbCopy,  TbCheck,  TbExternalLink,  TbSend,  TbX,  TbLoader2,} from 'react-icons/tb';import { GlassCard } from '@/components/ui/GlassCard';import { useTranslation } from '@/hooks/useTranslation';import SectionPulse from '@/components/effects/SectionPulse';import SakuraFall from '@/components/effects/SakuraFall';/* ------------------------------------------------------------------ *//* Toast                                                                *//* ------------------------------------------------------------------ */function Toast({ visible }: { visible: boolean }) {  return (    <AnimatePresence>      {visible && (        <motion.div          initial={{ opacity: 0, y: 16 }}          animate={{ opacity: 1, y: 0 }}          exit={{ opacity: 0, y: 16 }}          transition={{ duration: 0.25 }}          className="fixed bottom-8 left-1/2 -translate-x-1/2 z-50 flex items-center gap-2 px-4 py-2.5 rounded-full border border-accent-gold/40 bg-accent-gold/15 text-accent-gold text-sm font-medium backdrop-blur-md shadow-lg"        >          <TbCheck size={16} /> Copied! ✓        </motion.div>      )}    </AnimatePresence>  );}/* ------------------------------------------------------------------ *//* Field-level error message                                            *//* ------------------------------------------------------------------ */function FieldError({ msg }: { msg?: string }) {  return (    <AnimatePresence>      {msg && (        <motion.p          initial={{ opacity: 0, y: -4 }}          animate={{ opacity: 1, y: 0 }}          exit={{ opacity: 0, y: -4 }}          transition={{ duration: 0.18 }}          className="text-xs mt-1"          style={{ color: '#f4b8c1' }}        >          {msg}        </motion.p>      )}    </AnimatePresence>  );}/* ------------------------------------------------------------------ *//* Error banner                                                         *//* ------------------------------------------------------------------ */function ErrorBanner({ message, onDismiss }: { message: string; onDismiss: () => void }) {  return (    <motion.div      initial={{ opacity: 0, y: -8 }}      animate={{ opacity: 1, y: 0 }}      exit={{ opacity: 0, y: -8 }}      transition={{ duration: 0.22 }}      className="flex items-center gap-3 px-4 py-3 rounded-xl border border-accent-pink/30 bg-accent-pink/10 text-accent-pink text-sm mb-4"    >      <TbX size={16} className="shrink-0" />      <span className="flex-1">{message}</span>      <button onClick={onDismiss} className="shrink-0 opacity-60 hover:opacity-100 transition-opacity">        <TbX size={14} />      </button>    </motion.div>  );}/* ------------------------------------------------------------------ *//* Validation helpers                                                   *//* ------------------------------------------------------------------ */const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;interface FormFields { name: string; email: string; message: string; }interface FormErrors { name?: string; email?: string; message?: string; }function validate(fields: FormFields, t: (key: string) => unknown): FormErrors {  const errors: FormErrors = {};  if (!fields.name.trim()) {    errors.name = t('contact.form.name_error_required') as string;  } else if (fields.name.trim().length < 2) {    errors.name = t('contact.form.name_error_min') as string;  }  if (!fields.email.trim()) {    errors.email = t('contact.form.email_error_required') as string;  } else if (!EMAIL_RE.test(fields.email)) {    errors.email = t('contact.form.email_error_invalid') as string;  }  if (!fields.message.trim()) {    errors.message = t('contact.form.message_error_required') as string;  } else if (fields.message.trim().length < 10) {    errors.message = t('contact.form.message_error_min') as string;  }  return errors;}/* ------------------------------------------------------------------ *//* Main Section                                                         *//* ------------------------------------------------------------------ */export default function ContactSection() {  const { t } = useTranslation();  /* copy-email toast */  const [toastVisible, setToastVisible] = useState(false);  const toastTimer = useRef<ReturnType<typeof setTimeout> | null>(null);  /* form fields */  const [fields, setFields] = useState<FormFields>({ name: '', email: '', message: '' });  const [fieldErrors, setFieldErrors] = useState<FormErrors>({});  /* submission state */  const [isLoading, setIsLoading]     = useState(false);  const [formStatus, setFormStatus]   = useState<'idle' | 'success' | 'error'>('idle');  const [errorMessage, setErrorMessage] = useState('');  const errorTimer = useRef<ReturnType<typeof setTimeout> | null>(null);  /* auto-dismiss error banner after 5 s */  useEffect(() => {    if (formStatus === 'error') {      errorTimer.current = setTimeout(() => setFormStatus('idle'), 5000);    }    return () => { if (errorTimer.current) clearTimeout(errorTimer.current); };  }, [formStatus]);  const copyEmail = async () => {    await navigator.clipboard.writeText('akhdanravy@gmail.com');    setToastVisible(true);    if (toastTimer.current) clearTimeout(toastTimer.current);    toastTimer.current = setTimeout(() => setToastVisible(false), 2000);  };  const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => {    const { name, value } = e.target;    setFields((prev) => ({ ...prev, [name]: value }));    /* clear field error on edit */    if (fieldErrors[name as keyof FormErrors]) {      setFieldErrors((prev) => ({ ...prev, [name]: undefined }));    }  };  const handleSubmit = async (e: React.FormEvent<HTMLFormElement>) => {    e.preventDefault();    const errors = validate(fields, t);    if (Object.keys(errors).length > 0) {      setFieldErrors(errors);      return;    }    setIsLoading(true);    setFormStatus('idle');    try {      const res = await fetch('/api/contact', {        method: 'POST',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify(fields),      });      if (res.ok) {        setFormStatus('success');        setFields({ name: '', email: '', message: '' });      } else {        const data = await res.json().catch(() => ({}));        setErrorMessage(          data?.error ?? (t('contact.form.error_generic') as string),        );        setFormStatus('error');      }    } catch {      setErrorMessage(t('contact.form.error_generic') as string);      setFormStatus('error');    } finally {      setIsLoading(false);    }  };  const resetForm = () => {    setFormStatus('idle');    setFields({ name: '', email: '', message: '' });    setFieldErrors({});    setErrorMessage('');  };  /* shared input style helpers */  const inputBase: React.CSSProperties = {    background: 'rgba(255,255,255,0.05)',    border: '1px solid rgba(255,255,255,0.10)',    borderRadius: '8px',    padding: '12px 16px',    color: '#f0f0f5',    fontSize: '14px',    width: '100%',    outline: 'none',    transition: 'border-color 0.2s, background 0.2s',    opacity: isLoading ? 0.6 : 1,    cursor: isLoading ? 'not-allowed' : 'text',  };  const inputError: React.CSSProperties = { borderColor: '#f4b8c1' };  return (    <section id="contact" className="relative py-16 md:py-20 lg:py-24 px-4 overflow-hidden">      <SectionPulse variant="pink-pink" topLeft="45%" topRight="55%" scale={0.9} />      <SakuraFall />      {/* ── background kanji watermark ──────────────────────────── */}      <span        aria-hidden        className="pointer-events-none select-none absolute inset-0 flex items-center justify-center font-heading text-[20vw] font-bold text-accent-gold opacity-[0.04] leading-none"        style={{ writingMode: 'horizontal-tb', zIndex: 0 }}      >        連絡      </span>      <div className="relative z-10 max-w-6xl mx-auto px-6 lg:px-8">        {/* ── section label ─────────────────────────────────── */}        <motion.div          initial={{ opacity: 0, y: 20 }}          whileInView={{ opacity: 1, y: 0 }}          viewport={{ once: true }}          transition={{ duration: 0.5 }}          className="flex items-center gap-3 mb-4"        >          <span className="text-xs tracking-[0.2em] uppercase text-accent-pink font-medium">            07 / CONTACT          </span>          <span className="h-px w-10 bg-accent-pink/40" />        </motion.div>        {/* ── heading ─────────────────────────────────────────── */}        <motion.h2          initial={{ opacity: 0, y: 20 }}          whileInView={{ opacity: 1, y: 0 }}          viewport={{ once: true }}          transition={{ duration: 0.5, delay: 0.1 }}          className="font-heading text-4xl md:text-5xl font-bold text-white mb-3"        >          {t('contact.title') as string}        </motion.h2>        <motion.p          initial={{ opacity: 0, y: 20 }}          whileInView={{ opacity: 1, y: 0 }}          viewport={{ once: true }}          transition={{ duration: 0.5, delay: 0.15 }}          className="text-white/55 mb-12 max-w-xl"        >          {t('contact.subtitle') as string}        </motion.p>        {/* ── contact cards ─────────────────────────────────── */}        <div className="grid grid-cols-1 sm:grid-cols-2 gap-5 mb-12">          {/* Email card */}          <motion.div            initial={{ opacity: 0, y: 30 }}            whileInView={{ opacity: 1, y: 0 }}            viewport={{ once: true }}            transition={{ duration: 0.5, delay: 0.2 }}          >            <GlassCard glow className="h-full">              <div className="flex items-start gap-4">                <div className="w-11 h-11 rounded-xl flex items-center justify-center bg-accent-pink/10 border border-accent-pink/20 shrink-0">                  <TbMail size={20} className="text-accent-pink" />                </div>                <div className="flex-1 min-w-0">                  <p className="text-xs text-white/40 uppercase tracking-widest mb-1">                    {t('contact.email_label') as string}                  </p>                  <p className="text-sm text-white font-medium truncate mb-3">                    akhdanravy@gmail.com                  </p>                  <button                    onClick={copyEmail}                    className="flex items-center gap-1.5 text-xs px-3 py-1.5 rounded-lg border border-white/10 bg-white/5 text-white/60 hover:text-white hover:bg-white/10 transition-colors"                  >                    <TbCopy size={13} /> Copy address                  </button>                </div>              </div>            </GlassCard>          </motion.div>          {/* LinkedIn card */}          <motion.div            initial={{ opacity: 0, y: 30 }}            whileInView={{ opacity: 1, y: 0 }}            viewport={{ once: true }}            transition={{ duration: 0.5, delay: 0.3 }}          >            <GlassCard glow className="h-full">              <div className="flex items-start gap-4">                <div className="w-11 h-11 rounded-xl flex items-center justify-center bg-blue-400/10 border border-blue-400/20 shrink-0">                  <TbBrandLinkedin size={20} className="text-blue-300" />                </div>                <div className="flex-1 min-w-0">                  <p className="text-xs text-white/40 uppercase tracking-widest mb-1">                    {t('contact.linkedin_label') as string}                  </p>                  <p className="text-sm text-white font-medium mb-3">Akhdan Ravi Andaman</p>                  <a                    href="https://www.linkedin.com/in/akhdan-ravi-andaman/"                    target="_blank"                    rel="noopener noreferrer"                    className="flex items-center gap-1.5 text-xs px-3 py-1.5 rounded-lg border border-white/10 bg-white/5 text-white/60 hover:text-white hover:bg-white/10 transition-colors w-fit"                  >                    <TbExternalLink size={13} /> Open profile                  </a>                </div>              </div>            </GlassCard>          </motion.div>        </div>        {/* ── contact form / success state ─────────────────────── */}        <motion.div          initial={{ opacity: 0, y: 30 }}          whileInView={{ opacity: 1, y: 0 }}          viewport={{ once: true }}          transition={{ duration: 0.55, delay: 0.2 }}        >          <AnimatePresence mode="wait">            {formStatus === 'success' ? (              /* ── Success card ────────────────────────────────── */              <motion.div                key="success"                initial={{ opacity: 0, scale: 0.88 }}                animate={{ opacity: 1, scale: 1 }}                exit={{ opacity: 0, scale: 0.88 }}                transition={{ duration: 0.35, ease: 'easeOut' }}              >                <GlassCard>                  <div className="flex flex-col items-center text-center py-8">                    <div className="w-16 h-16 rounded-full flex items-center justify-center bg-accent-gold/10 border border-accent-gold/30 mb-5">                      <TbCheck size={32} className="text-accent-gold" />                    </div>                    <h3 className="font-heading text-2xl font-bold text-white mb-3">                      {t('contact.form.success_title') as string}                    </h3>                    <p className="text-white/60 text-sm leading-relaxed max-w-sm">                      {(t('contact.form.success_message') as string).replace('{name}', fields.name || 'there')}                    </p>                    <button                      onClick={resetForm}                      className="mt-7 flex items-center gap-2 px-6 py-2.5 rounded-xl border border-accent-gold/40 bg-accent-gold/10 text-accent-gold text-sm font-medium hover:bg-accent-gold/20 transition-colors"                    >                      <TbSend size={15} />                      {t('contact.form.send_another') as string}                    </button>                  </div>                </GlassCard>              </motion.div>            ) : (              /* ── Form ───────────────────────────────────────── */              <motion.div key="form" initial={{ opacity: 1 }} exit={{ opacity: 0 }}>                <GlassCard>                  {/* Error banner */}                  <AnimatePresence>                    {formStatus === 'error' && (                      <ErrorBanner                        message={errorMessage}                        onDismiss={() => setFormStatus('idle')}                      />                    )}                  </AnimatePresence>                  <form onSubmit={handleSubmit} className="space-y-5" noValidate>                    <div className="grid grid-cols-1 sm:grid-cols-2 gap-5">                      {/* Name */}                      <div className="flex flex-col gap-1">                        <label className="text-xs text-white/50 uppercase tracking-widest mb-0.5">                          {t('contact.form.name') as string}                        </label>                        <input                          type="text"                          name="name"                          value={fields.name}                          onChange={handleChange}                          disabled={isLoading}                          placeholder={t('contact.form.name_placeholder') as string}                          style={{                            ...inputBase,                            ...(fieldErrors.name ? inputError : {}),                          }}                          onFocus={(e) => {                            if (!fieldErrors.name)                              e.target.style.borderColor = '#c9a84c';                          }}                          onBlur={(e) => {                            if (!fieldErrors.name)                              e.target.style.borderColor = 'rgba(255,255,255,0.10)';                          }}                        />                        <FieldError msg={fieldErrors.name} />                      </div>                      {/* Email */}                      <div className="flex flex-col gap-1">                        <label className="text-xs text-white/50 uppercase tracking-widest mb-0.5">                          {t('contact.form.email') as string}                        </label>                        <input                          type="email"                          name="email"                          value={fields.email}                          onChange={handleChange}                          disabled={isLoading}                          placeholder={t('contact.form.email_placeholder') as string}                          style={{                            ...inputBase,                            ...(fieldErrors.email ? inputError : {}),                          }}                          onFocus={(e) => {                            if (!fieldErrors.email)                              e.target.style.borderColor = '#c9a84c';                          }}                          onBlur={(e) => {                            if (!fieldErrors.email)                              e.target.style.borderColor = 'rgba(255,255,255,0.10)';                          }}                        />                        <FieldError msg={fieldErrors.email} />                      </div>                    </div>                    {/* Message */}                    <div className="flex flex-col gap-1">                      <label className="text-xs text-white/50 uppercase tracking-widest mb-0.5">                        {t('contact.form.message') as string}                      </label>                      <textarea                        name="message"                        value={fields.message}                        onChange={handleChange}                        disabled={isLoading}                        rows={5}                        placeholder={t('contact.form.message_placeholder') as string}                        style={{                          ...inputBase,                          resize: 'none',                          ...(fieldErrors.message ? inputError : {}),                        }}                        onFocus={(e) => {                          if (!fieldErrors.message)                            e.target.style.borderColor = '#c9a84c';                        }}                        onBlur={(e) => {                          if (!fieldErrors.message)                            e.target.style.borderColor = 'rgba(255,255,255,0.10)';                        }}                      />                      <FieldError msg={fieldErrors.message} />                    </div>                    {/* Submit */}                    <div className="flex justify-end">                      <button                        type="submit"                        disabled={isLoading}                        className="flex items-center gap-2 px-6 py-2.5 rounded-xl bg-accent-gold text-bg font-semibold text-sm transition-all disabled:opacity-60 disabled:cursor-not-allowed hover:brightness-110 active:scale-95"                      >                        {isLoading ? (                          <>                            <TbLoader2 size={16} className="animate-spin" />                            {t('contact.form.sending') as string}                          </>                        ) : (                          <>                            <TbSend size={16} />                            {t('contact.form.submit') as string}                          </>                        )}                      </button>                    </div>                  </form>                </GlassCard>              </motion.div>            )}          </AnimatePresence>        </motion.div>      </div>      {/* ── copy-email toast ─────────────────────────────────── */}      <Toast visible={toastVisible} />    </section>  );}

